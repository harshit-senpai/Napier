"use client";

import { Button } from "@/components/ui/button";
import { Plus } from "lucide-react";

export const WorkflowButton = () => {
  return (
    <Button size={"icon"}>
      <Plus />
    </Button>
  );
};
